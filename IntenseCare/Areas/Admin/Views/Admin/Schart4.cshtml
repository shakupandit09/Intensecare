@{
    var mychart = new Chart(width: 600, height: 300, theme: ChartTheme.Green)
            .AddTitle("InhouseTreatement Patients List")
            .AddSeries(
    name: "Inhouse Patient",
    xValue: ViewBag.x,
    yValues: ViewBag.y).Write();
}

